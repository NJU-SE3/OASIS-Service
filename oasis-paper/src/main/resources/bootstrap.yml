spring:
  application:
    name: client
  cloud:
    config:
      profile: dev
      uri: http://localhost:8090/
      discovery:
        enabled: true
        service-id: config-server
      name: client
eureka:
  port: 8761